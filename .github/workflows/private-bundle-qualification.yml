name: Private bundle qualification (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - run: |
          echo "QUALIFICATION_STAGE=archived"
          echo "Use .github/workflows/private-bundle-integration.yml for owner-only issue-triggered bundle execution."
